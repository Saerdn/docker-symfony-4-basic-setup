# docker-symfony-4-basic-setup
Basic Symfony 4 setup with Docker, containing an admin layout (3rd party), two entities (parent + child), basic user mannagement (admin/user) and login/authorization implementation.
